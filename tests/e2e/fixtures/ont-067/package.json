{
  "name": "ont-067-fixture-shop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "e2e fixture repo for ONT-067: one datasource and two models, composed with a generator block per phase, copied OUT OF REPO so the Prisma-major probe reads the node_modules the scenario plants rather than the monorepo's own."
}
